import { addIcons } from "ionicons";
import { carOutline, homeOutline, qrCodeOutline } from "ionicons/icons";

export function loadIcons() {
    addIcons({
        homeOutline,
        carOutline,
        qrCodeOutline,
    });
}