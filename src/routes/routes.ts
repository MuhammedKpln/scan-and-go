export enum Routes {
  Root = "/",
  AppRoot = "/app",
  Home = "/app/home",
  Notes = "/app/notes",
  Scan = "/app/scan",
  Chats = "/app/chats",
  Profile = "/app/profile",
  Login = "/login",
  Register = "/register",
  Settings = "/settings",
  Tag = "/tag/:tagUid",
}
